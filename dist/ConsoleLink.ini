;----------------------------------------------------------------------------------------------
; Console Link configuration file


;----------------------------------------------------------------------------------------------
; Primary console feature toggles
[Main]

; Mirror accepted console input into ConsoleLink.activity.log (default: true)
EnableConsoleInputLogging = true

; Mirror engine console output into ConsoleLink.activity.log (default: true)
EnableConsoleOutputLogging = true

; Allow inbox lines to be submitted as console commands (default: true)
EnableInbox = true


;----------------------------------------------------------------------------------------------
; Inbox
[Inbox]

; File watched for inbox console commands.
; Relative paths resolve under the standard SKSE log directory.
; Default: ConsoleLink.inbox.txt
Path = ConsoleLink.inbox.txt

; Maximum inbox commands to run before yielding back to the game (default: 1)
MaxLinesPerTick = 1
